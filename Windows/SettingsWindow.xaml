<Window x:Class="JustAStopwatch.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pomodoro Preferences" Height="150" Width="250"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Topmost="True">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Work Duration (mins):" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Margin="0,5"/>
        <TextBox x:Name="WorkBox" Grid.Row="0" Grid.Column="1" Margin="0,5" VerticalContentAlignment="Center"/>

        <TextBlock Text="Break Duration (mins):" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" Margin="0,5"/>
        <TextBox x:Name="BreakBox" Grid.Row="1" Grid.Column="1" Margin="0,5" VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="60" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="60" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
